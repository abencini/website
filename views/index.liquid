{% block content %}

<section id="projects">
    <h1>{{ model.sections["projects"].title }}</h1>
    {% collection "projects" query 'featured=true' order desc %}
        {% include "showcase" %}
    {% endcollection %}
</section>

<section id="clients">
    <h1>{{ model.sections["clients"].title }}</h1>
    {% collection "clients" query 'featured=true' order desc %}
        {% include "showcase" %}
    {% endcollection %}
</section>

{% endblock %}
